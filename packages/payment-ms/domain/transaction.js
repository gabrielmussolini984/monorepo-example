// const checkParamsSubscriptionRequest = (request) => {
//   // Utilizar o Joi p/ valdiar.
//   const { secret_key, gateway, api_key, plan_id } = request;
//   if (!secret_key || !gateway || !api_key || !plan_id)
//     throw 'Erro na validação';
//   return request;
// };

// module.exports = {
//   checkParamsSubscriptionRequest
// };
