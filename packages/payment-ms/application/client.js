// module.exports = {
//   getClientByKey: async (secret_key) => {
//     try {
//       // Verifica se o cliente existe, se não existir retorna um throw é utilizado o dynamoDB
//       if (secret_key !== "200") throw "Cliente não encontrado";
//       return true;
//     } catch (error) {
//       throw error;
//     }
//   }
// }
