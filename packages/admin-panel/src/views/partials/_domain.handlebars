/* Import domain */
let domain;
{{#if tenant.domain}}
domain = "{{tenant.domain}}";
{{else}}
  {{#if tenant.is_admin}}
  domain = "http://{{tenant.fallback_subdomain}}.superstoree.test:3000";
  {{else}}
  domain = "http://{{tenant.fallback_subdomain}}.admin.superstoree.test:3000";
  {{/if}}
{{/if}}

const configTable = {
  serverSide: true,
  responsive: true,
  autoWidth: false,
  searching: false,
  ordering: false,
  language: {
    lengthMenu: "Mostrar _MENU_ dados por página",
    zeroRecords: "Nothing found - sorry",
    info: "Mostrando _PAGE_ de _PAGES_ páginas. Total de _MAX_ registros",
    infoEmpty: "No records available",
    infoFiltered: "(filtered from _MAX_ total records)",
    search: "Pesquisar",
    paginate: {
      next: "Próximo",
      previous: "Anterior",
    }
  },
};

/* /Import domain */